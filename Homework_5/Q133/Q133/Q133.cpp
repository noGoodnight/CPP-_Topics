#include<iostream>

using namespace std;

int main() {
	int a, b, c, d, e, f;
	
	cin >> a >> b >> c >> d >> e >> f;
	cout << "chery_market_size is: " << a * b + c * d + e * f << endl;

	cin >> a >> b >> c >> d >> e >> f;
	cout << "benz_market_size is: " << a * b + c * d + e * f << endl;

	cin >> a >> b >> c >> d >> e >> f;
	cout << "tesla_market_size is: " << a * b + c * d + e * f << endl;

	cin >> a >> b >> c >> d >> e >> f;
	cout << "model_car_market_size is: " << a * b + c * d + e * f;
}